function substring(stringInput, startIndex, numsCount){

    let result = stringInput.substr(startIndex, numsCount);

    console.log(result);
}

substring('ASentence', 1, 8);
substring('SkipWord', 4, 7);